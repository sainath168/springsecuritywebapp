# Below properties are helpful if we want to have use some default login details
# but this is not a production use case for an typical web app. Usually we authenticate
# user against list of users from DB (or at least in-memory DB)

# spring.security.user.name=admin
# spring.security.user.password=admin